use anchor_lang::prelude::*; // ::* = Wildcard import (brings everything from prelude into scope), 
                             // prelude = A special module containing commonly used items

declare_id!("");

pub const ANCHOR_DISCRIMINATOR_SIZE: usize = 8;